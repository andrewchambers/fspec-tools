#! /bin/sh
set -eux

mkdir foo
touch foo/a.txt
touch foo/b.sh
chmod +x foo/b.sh
mkfifo c

fspec-fromdir -r ./foo > a.fspec
tar -C foo -cf - a.txt b.sh c | fspec-fromtar > b.fspec
diff -u a.fspec b.fspec
