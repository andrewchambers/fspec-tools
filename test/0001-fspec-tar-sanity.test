#! /bin/sh

set -eux

cat <<EOF | fspec-tar | tar -xf -
foo
type=reg
mode=0700

bar
type=reg

baz
type=dir
EOF

test -f foo
test -f bar
test -d baz

test "0700" = "$(fmode foo)"
test "0644" = "$(fmode bar)"
test "0755" = "$(fmode baz)"
